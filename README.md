# 📋✅ ДЗ 50.1. Оптимізація React-додатків через мемоізацію

Цей проект демонструє використання механізмів мемоізації в React для оптимізації продуктивності додатку.

---

## Використані технології
* **useMemo** — для кешування результатів важких обчислень.
* **useCallback** — для збереження посилальної цілісності функцій між рендерами.

---

## Як працює оптимізація в цьому прикладі:

1. **`useMemo`**: Ми обгортаємо логіку фільтрації товарів. Це гарантує, що масив `filteredProducts` буде перераховуватися лише тоді, коли користувач вводить текст у пошук або змінює список товарів. Зміна стороннього лічильника (`count`) не викликає повторну фільтрацію.
   
2. **`useCallback`**: Функція `removeProduct` передається в дочірній компонент. Завдяки `useCallback`, посилання на цю функцію залишається стабільним. Це критично важливо для роботи наступного кроку.

---

## 🛠 Технологічний стек

* **React (Vite)** — сучасне середовище розробки з миттєвим оновленням модулів (HMR).
* **Vitest** — швидкий фреймворк для модульного тестування, інтегрований з Vite.
* **React Testing Library** — бібліотека для тестування компонентів через взаємодію з DOM.
* **Fetch API** — використання стандартних браузерних методів для отримання даних.

---

## 🔗 Посилання
- **Демо проекту (Live Demo):** [react-lesson-50.vercel.app]
- **Репозиторій:** [https://github.com/Viktoria057/react-lesson-50.git]

---

## Структура проєкту 📂

````
my-react-app/
├── src/
│ ├── components/
│ │  └── Calculator.jsx
│ ├── App.jsx
│ ├── main.jsx
│ └── index.css
├── package.json
├── README.md
└── ...
````
